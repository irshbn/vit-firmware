[Unit]
Description=Start uDHCP server
After=network.target

[Service]
Type=idle
ExecStart=/usr/bin/sh -c "ifconfig end0 192.168.11.1 && /usr/bin/touch /var/udhcpd.leases && /usr/sbin/udhcpd -f /etc/udhcpd.conf"

[Install]
WantedBy=multi-user.target
